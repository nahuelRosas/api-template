/**
 * Represents the type of a vehicle.
 */
export enum VehicleType {
  SEDAN = 'Sedan',
  SUV = 'SUV',
  TRUCK = 'Truck',
  VAN = 'Van',
  CONVERTIBLE = 'Convertible',
  COUPE = 'Coupe',
  HATCHBACK = 'Hatchback',
}

/**
 * Represents the type of transmission for a car.
 */
export enum TransmissionType {
  AUTOMATIC = 'Automatic',
  MANUAL = 'Manual',
}

/**
 * Represents the condition of a vehicle.
 */
export enum VehicleCondition {
  NEW = 'New',
  USED = 'Used',
  DAMAGED = 'Damaged',
}

/**
 * Represents the type of fuel for a car.
 */
export enum FuelType {
  GASOLINE = 'Gasoline',
  DIESEL = 'Diesel',
  ELECTRIC = 'Electric',
  HYBRID = 'Hybrid',
  GAS = 'Gas',
}
